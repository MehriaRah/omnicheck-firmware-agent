# omnicheck-firmware-agent
An autonomous embedded firmware auditing agent using the Google GenAI SDK (Gemini 2.5 Flash) to inspect code, detect hardware policy violations, auto-apply patches, and run static compilation checks.
